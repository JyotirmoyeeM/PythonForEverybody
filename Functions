def computepay(h, r):
    if h<=40:
        p= (h*r)
    elif h > 40:
        p= x*r + ((h-x)*r*1.5)
    return p

hrs = input("Enter Hours:")
h=float(hrs)
rate =input("Enter Rate per hours:")
r=float(rate)
x=40

p = computepay(h, r)
print("Pay", p)
